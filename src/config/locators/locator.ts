export class Locator {
  static readonly topMenuOptions = '.navbar li a';
  static readonly products = 'app-card';
  static readonly addButton = 'button';
  static readonly checkoutButton = '.navbar-collapse .btn-primary';
  static readonly checkoutProducts = '.table-hover tr';
  static readonly removeButton = '.btn-danger';
}
